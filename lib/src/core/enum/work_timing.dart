enum WorkTiming {
  daily, //not need to select any date time info
  weekly, //need select weekday as sunday , monday ..etc.
  dayInmonth, //need select month and day
  dailyInMonth, //need select month only
  weeklyInMonth, //need select weekday and month
  oneWeekDayInMonth //need select weekday and month and day number mean the serise of this week day in month
}
